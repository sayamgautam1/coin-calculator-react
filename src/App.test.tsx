import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {
  // render(<App />);
  // const linkElement = screen.getByText(/learn react/i);
  // expect(linkElement).toBeInTheDocument();
  // find input element
  // inputEl = screen.getbyrole('input', {id: 'coin-input'})
  // enter amount
  // inputEl.type('10.23')
  // find button
  // btn = screen.getByRole('button', { type: 'submit'})
  // btn.click()
  // check valid output
  // find <ul> <li data-testid='coin-2d'>image of 2d coin</li> ...other coins
  // const coin2d =screen.getbytestid('coin-2d')
  // expect(coin2d).textContent.toBe(5)
});
